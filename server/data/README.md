# Data Directory

This directory stores cached data:

- `cache.json` - Cached podcast transcripts and metadata
- `summaries.json` - Saved AI summaries

These files are gitignored as they contain user-specific data and can get large.
The app will create them automatically when needed.
